# Matches the documented local port (CLAUDE.md); avoids colliding with auth on 8080.
server.port=8001

# Shared Postgres DB; this service is isolated by its own `email` schema.
spring.datasource.url=jdbc:postgresql://localhost:5432/userdb
spring.datasource.username=user
spring.datasource.password=user123

auth.jwks-url=http://localhost:8080/api/v1/auth/.well-known/jwks.json

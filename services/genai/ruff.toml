extend = "../../ruff.toml"
# client/ is OpenAPI-generated (make -C api generate) — never lint or format it.
extend-exclude = ["client"]

[lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends"]

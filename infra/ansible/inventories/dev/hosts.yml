# The controller talks to the AKS API via kubeconfig — no remote hosts.
all:
  hosts:
    controller:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
